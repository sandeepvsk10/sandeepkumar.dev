export default function OpenSourcePage() {
  return (
    <main className="flex w-full flex-1 flex-col py-12">
      <p className="text-muted-foreground">
        Content for open source projects goes here — lists, cards, or MDX
        later.
      </p>
    </main>
  );
}
